services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: book_store
      POSTGRES_USER: bookstore
      POSTGRES_PASSWORD: bookstore
    ports: ["5432:5432"]
    volumes: [bookstore-postgres:/var/lib/postgresql/data]
volumes:
  bookstore-postgres:
